{"result":"findings","findings":[{"severity":"high","issue":"continue 的“无当前确认不得 promote/delete/进入 overview/detail/start”未排除“陈旧确认”：若上一回合已写入带 user_quote 的 user_confirmed 记录，continue 可能复用该历史记录当作有效确认，从而在本回合无新确认时提权。","fix":"明确“current confirmation”= 当前回合用户输入产生的 user_quote/confirmed_ref，禁止读取既存 user_confirmed 状态作为放行依据；review/gate/tests 增加负向用例：仅存历史确认、本回合无引用时，promote/start 必须被拒。"},{"severity":"high","issue":"batch 例外是“每回合一个高风险问题/一次 next_action”的唯一缺口，但“当前用户明确确认多个并记录覆盖的 OQ ids”判定若放宽（如把含糊措辞当确认），可一次性提权多个 OQ，绕过单问循环。","fix":"将 batch 门槛收紧为：本回合用户引用须逐一枚举被覆盖的 OQ id 且记录 user_quote；不满足时强制回退为一个 next_action、其余 OQ 置 open；manual checklist + tests 覆盖“模糊批量确认应回退单问”。"},{"severity":"medium","issue":"requirement-writing 仅可起草 ai_drafted/evidence_ready/open 且 evidence_ready≠确认，但缺少防止 evidence_ready 被静默升级为 user_confirmed、以及 user_confirmed 缺 user_quote/confirmed_ref 被接受的强制校验测试。","fix":"在 gate/tests 增加断言:evidence_ready 不可自动转 user_confirmed;user_confirmed 缺 user_quote 且缺 confirmed_ref 时必须拒绝并报错,纳入 manual checklist。"}],"machine":"review_result=findings route_class=requirements max_severity=high"}
