# Naming Conventions

> 本项目文件、类型、成员、常量的实际命名规律。

---

## File & Directory Names

<!-- 文件名与目录名怎么命名（kebab-case vs camelCase；route 段固定名 `page`/`layout`/`loading`/`error`；组件文件后缀约定）？`app/` 下目录如何按 route 段与 colocation 分组？ -->

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Types & Components

<!-- 组件、type/interface、enum、zod schema 怎么命名（PascalCase）？是否有领域后缀约定（Props/Schema/Dto/Type）？`client-component` 与 `server-component` 命名上如何区分？ -->

```ts
// WRONG:

// CORRECT:
```

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Variables & Functions

<!-- 变量、函数、hook（`useXxx`）、server-action、事件处理器（`onXxx`/`handleXxx`）、布尔值（is/has/should）怎么命名？私有/内部成员如何标注？ -->

```ts
// WRONG:

// CORRECT:
```

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Constants & Env Vars

<!-- 常量、枚举值、静态配置怎么命名（SCREAMING_SNAKE_CASE vs camelCase）？环境变量命名与 `NEXT_PUBLIC_` 前缀边界（仅可公开值带前缀）如何约束？ -->

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Common Mistakes

<!-- 命名上反复出现的不一致或歧义有哪些（route 段错名、secret 误带 NEXT_PUBLIC_、组件/hook 大小写混用等）？review 时高频纠正的命名问题？ -->

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)
