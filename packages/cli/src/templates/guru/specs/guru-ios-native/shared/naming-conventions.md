# Naming Conventions

> 本项目文件、类型、成员、常量的实际命名规律。

---

## File & Directory Names

<!-- 文件名与目录名怎么命名（`UpperCamelCase.swift`、后缀如 `+Container`/`View`/`ViewModel`/`Repository`）？四层目录与 feature 子目录如何分组？ -->

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Types & Protocols

<!-- struct/class/enum/protocol 怎么命名？协议用 `IXxx` 前缀还是 `XxxProtocol` 后缀（见 conventions SLOT-03）？是否有领域后缀约定（Model/Entity/Error/State/DTO）？ -->

```swift
// WRONG:

// CORRECT:
```

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Properties & Methods

<!-- 属性、方法、私有成员、布尔值（is/has/should/can）怎么命名？参数标签与省略词（`with`/`for`/`from`）的约定是什么？ -->

```swift
// WRONG:

// CORRECT:
```

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Constants & Enum Cases

<!-- 常量、枚举 case、静态配置、命名空间（`enum` 当 namespace）的命名约定是什么（lowerCamelCase 还是其它）？ -->

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)

---

## Common Mistakes

<!-- 命名上反复出现的不一致或歧义有哪些（缩写大小写、`I` 前缀漏写、后缀串台等）？review 时高频纠正的命名问题？ -->

(To be filled by bootstrap — 见 00-bootstrap-guidelines 任务)
