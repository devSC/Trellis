{
  "_comment": "合并到目标仓库 .codex/hooks.json。Codex hooks 还需要用户级 ~/.codex/config.toml 启用 [features].hooks=true，并在 /hooks TUI 中 trust 本项目 hook。",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash .codex/hooks/block-unstarted-commit.sh",
            "timeoutSec": 15
          }
        ]
      }
    ]
  }
}
